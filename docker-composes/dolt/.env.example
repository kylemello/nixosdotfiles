# Copy to .env and fill in. .env is gitignored — this repo is public.
# Every value below has a dev default in docker-compose.yaml; override here.
DOLT_ROOT_PASSWORD=
DOLT_USER=devuser
DOLT_PASSWORD=
DOLT_DATABASE=DocResolve
DOLT_PORT=3306
